{
  "name": "qltc",
  "private": true,
  "scripts": {
    "build": "node build-env.js && mkdir -p dist && cp -r js css *.html dist",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4"
  }
}