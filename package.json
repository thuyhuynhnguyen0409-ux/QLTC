{
  "name": "qltc",
  "private": true,
  "scripts": {
  "build": "node build-env.js && cp -r css *.html dist/ 2>/dev/null || :",
  "vercel-build": "npm run build"
},
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4"
  }
}